import { colors } from "@/infra/colors";
import { fonts, fontWeights, fontSizes } from "@/infra/fonts";
import { sizes } from "@/infra/sizes";
import { space, lineHeights } from "@/infra/spacing";

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeights
};
